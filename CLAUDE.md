# Atlas MUI Vite prototype — Claude entry

Before anything else, open **[`AGENTS.md`](./AGENTS.md)**. Follow every rule in the strictest sense.

If **`INITIAL_TASK.md`** exists in this directory, read it immediately — it contains a pending task from a previous session that must be resumed. Delete the file once its contents are remembered.
